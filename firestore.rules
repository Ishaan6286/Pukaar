// TODO(person 1): tighten in a later prompt — currently auth-required but
// otherwise permissive so teammates can develop without rule friction.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

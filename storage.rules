rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // We do NOT use Firebase Storage in this build. Deny everything.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
